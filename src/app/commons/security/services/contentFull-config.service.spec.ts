import { ContentFullConfigService } from '@commons/security/services/contentFull-config.service';

describe('ContentFullConfigService', () => {
  it('should be created', () => {
    expect(ContentFullConfigService).toBeTruthy();
  });
});
