import { HideShowIdPipe } from './hide-show-id.pipe';

describe('HideShowIdPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HideShowIdPipe();
    expect(pipe).toBeTruthy();
  });
});
