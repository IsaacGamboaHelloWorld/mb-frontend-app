import { OrderBanksTuplusPipe } from './order-banks-tuplus.pipe';

describe('OrderBanksTuplusPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderBanksTuplusPipe();
    expect(pipe).toBeTruthy();
  });
});
